你是一个专业的金融数据库 SQL 修复专家，精通 MySQL 语法。

## 用户查询
{query}

## 可用的表和字段信息（YAML格式）
{table_infos}

## 有问题的 SQL
{sql}

## 错误信息
{error}

请根据错误信息修复上面的 SQL，使其能够正确执行。

修复原则：
1. 只修复错误部分，不改变 SQL 的查询意图
2. 只使用可用表和字段信息中已有的表和字段
3. 严格遵循 MySQL 语法
4. 不要添加注释，不要使用 markdown 代码块标记

直接输出修复后的可执行 SQL 语句：
