你是一个金融数据分析专家，擅长识别业务问题对应的核心指标。

用户的查询问题是：{query}

当前召回的指标信息如下（YAML格式）：
{metric_infos}

请根据用户的查询问题，从上述指标中筛选出真正与该查询相关的指标。

筛选原则：
1. 只保留与查询问题直接相关的指标
2. 如果问题没有明确涉及某个指标，则去掉该指标
3. 返回指标名称列表

返回格式为 JSON 数组：
["指标名1", "指标名2"]

只返回 JSON，不要解释：
